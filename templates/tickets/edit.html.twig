{% extends 'base.htm
    <a href="{{ path('tickets_edit', {'id': ticket.id}) }}">edit</a>l.twig' %}

{% block title %}Edit Tickets{% endblock %}

{% block body %}
    <h1>Edit Tickets</h1>

    {{ include('tickets/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('tickets_index') }}">back to list</a>

    {{ include('tickets/_delete_form.html.twig') }}
{% endblock %}
